/* 页面容器 - 老年用户优化 */
.container {
  padding: 30rpx; /* 增大内边距 */
  padding-bottom: 100rpx; /* 进一步增大底部内边距，为按钮提供更多空间 */
  min-height: 120vh;
  background: #FFFFFF; /* 纯色背景 */
  display: flex;
  flex-direction: column;
  gap: 30rpx; /* 增大间距 */
  justify-content: space-between;
}

/* 三段式布局 */
.guide-section {
  text-align: center;
  padding: 20rpx 0;
  margin-bottom: 10rpx;
}

.guide-text {
  font-size: 32rpx;
  color: #3D3D3D;
  font-family: 'Source Han Serif SC', 'KaiTi', '楷体', serif;
  margin-bottom: 15rpx;
  text-shadow: 1rpx 1rpx 2rpx rgba(0, 0, 0, 0.1);
}

.guide-subtitle {
  font-size: 26rpx;
  color: #5A5A5A;
  line-height: 1.4;
}

.form-section {
  flex: 1;
  overflow-y: auto;
}

.result-section {
  margin-top: auto;
  padding-top: 20rpx;
}

/* 卡片样式 */
.card-primary {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 20rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  box-shadow: 0 8rpx 25rpx rgba(0, 0, 0, 0.08), inset 0 1rpx 3rpx rgba(255, 255, 255, 0.5);
  position: relative;
  overflow: hidden;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  border: 1rpx solid rgba(107, 142, 35, 0.1);
}

.card-primary:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: 0 5rpx 15rpx rgba(0, 0, 0, 0.12), inset 0 1rpx 3rpx rgba(255, 255, 255, 0.3);
}

.card-secondary {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 20rpx;
  padding: 30rpx;
  box-shadow: 0 8rpx 25rpx rgba(0, 0, 0, 0.08), inset 0 1rpx 3rpx rgba(255, 255, 255, 0.5);
  position: relative;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  border: 1rpx solid rgba(107, 142, 35, 0.1);
  box-sizing: border-box; /* 确保padding不会增加元素总宽度 */
  width: 100%; /* 确保卡片占据容器的全部宽度 */
}

.card-secondary:active {
  transform: translateY(2rpx) scale(0.98);
  box-shadow: 0 5rpx 15rpx rgba(0, 0, 0, 0.12), inset 0 1rpx 3rpx rgba(255, 255, 255, 0.3);
}

/* 装饰元素 */
.decoration-tr {
  position: absolute;
  top: -20rpx;
  right: -20rpx;
  width: 100rpx;
  height: 100rpx;
  background: linear-gradient(45deg, #C79E6F, #A0522D);
  border-radius: 50%;
  opacity: 0.2;
}

/* 标题样式 - 老年用户优化 */
.title {
  font-size: 48rpx; /* 增大字体 */
  font-weight: bold; /* 加粗 */
  margin-bottom: 40rpx; /* 增大下边距 */
  color: #000000; /* 高对比度 */
  font-family: 'Source Han Serif SC', 'KaiTi', '楷体', serif;
  text-shadow: none; /* 移除阴影 */
}

.festive-title {
  color: #3D3D3D;
  font-family: 'Source Han Serif SC', 'KaiTi', '楷体', serif;
  text-shadow: 1rpx 1rpx 2rpx rgba(0, 0, 0, 0.1);
}

/* 模式选择器 */
.mode-selector {
  margin-bottom: 40rpx;
}

.mode-buttons {
  display: flex;
  justify-content: center;
  gap: 20rpx;
  flex-wrap: wrap;
}

.btn-mode {
  padding: 25rpx 35rpx; /* 增大按钮内边距 */
  border: none;
  border-radius: 50rpx;
  font-size: 32rpx; /* 增大字体 */
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  min-width: 220rpx; /* 增大最小宽度 */
  min-height: 100rpx; /* 设置最小高度，增大触摸区域 */
  box-shadow: 0 4rpx 15rpx rgba(0, 0, 0, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-mode-inactive {
  background: rgba(248, 240, 227, 0.8);
  color: #5A5A5A;
  border: 1rpx solid rgba(107, 142, 35, 0.2);
}

.btn-mode-active {
  color: white;
  transform: scale(1.05);
  box-shadow: 0 6rpx 20rpx rgba(0, 0, 0, 0.15), inset 0 1rpx 2rpx rgba(255, 255, 255, 0.2);
}

.btn-template-active {
  background: linear-gradient(to right, #6B8E23, #A0522D);
}

.btn-smart-active {
  background: linear-gradient(to right, #A0522D, #6B8E23);
}

/* 表单内容 */
.form-content {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.mode-content {
  flex: 1;
  animation: modeSlideIn 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes modeSlideIn {
  from {
    opacity: 0;
    transform: translateX(30rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* 输入组 */
.input-group {
  margin-bottom: 30rpx;
}

.input-label {
  display: block;
  font-size: 36rpx; /* 增大字体 */
  font-weight: bold;
  margin-bottom: 20rpx; /* 增大下边距 */
  color: #000000; /* 高对比度 */
}

/* 步骤指示器标题 */
.step-indicator-title {
  text-align: center;
  font-size: 36rpx;
  font-weight: bold;
  color: #000000;
  margin-bottom: 30rpx;
  padding: 0 20rpx;
}

/* 步骤指示器 */
.step-indicator {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 50rpx;
  padding: 0 20rpx;
}

.step-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  flex: 1;
}

.step-number {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background-color: #f0f0f0;
  color: #666;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: bold;
  font-size: 40rpx;
  margin-bottom: 0;
  box-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
}

.step-number:active {
  transform: scale(0.95);
}

.step-item.active .step-number {
  background: linear-gradient(45deg, #6B8E23, #A0522D);
  color: white;
  box-shadow: 0 6rpx 12rpx rgba(0, 0, 0, 0.15);
  transform: scale(1.1);
}

.step-separator {
  height: 2rpx;
  background-color: #e0e0e0;
  flex: 1;
  margin: 0 10rpx;
}

/* 步骤内容 */
.step-content {
  animation: fadeIn 0.5s ease-in-out;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateX(20rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* 步骤导航按钮 */
.step-navigation {
  display: flex;
  justify-content: space-between;
  margin-top: 50rpx;
}

.btn-step-prev, .btn-step-next {
  padding: 25rpx 40rpx;
  border: none;
  border-radius: 50rpx;
  font-size: 32rpx;
  font-weight: bold;
  cursor: pointer;
  transition: all 0.3s ease;
  min-width: 200rpx;
  min-height: 100rpx;
  box-shadow: 0 4rpx 15rpx rgba(0, 0, 0, 0.1);
  display: flex;
  align-items: center;
  justify-content: center;
}

.btn-step-prev {
  background: #f0f0f0;
  color: #333;
}

.btn-step-next {
  background: linear-gradient(45deg, #6B8E23, #A0522D);
  color: white;
}

.btn-step-prev:active, .btn-step-next:active {
  transform: scale(0.98);
}

.btn-step-next[disabled] {
  opacity: 0.6;
  cursor: not-allowed;
  background: #cccccc;
}

.textarea-input {
  width: 100%;
  height: 300rpx; /* 增大高度 */
  padding: 30rpx; /* 增大内边距 */
  border: 3rpx solid #CCCCCC; /* 高对比度边框 */
  border-radius: 20rpx; /* 增大圆角 */
  font-size: 36rpx; /* 增大字体 */
  background: #FFFFFF; /* 纯色背景 */
  box-sizing: border-box;
  transition: all 0.3s ease; /* 简化动画 */
  transform: scale(1);
  position: relative;
  box-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.1); /* 更明显阴影 */
}

.textarea-input::before {
  content: '';
  position: absolute;
  top: 10rpx;
  right: 10rpx;
  width: 30rpx;
  height: 30rpx;
  background: linear-gradient(-45deg, transparent 10rpx, rgba(160, 82, 45, 0.2) 10rpx, rgba(160, 82, 45, 0.2) 12rpx, transparent 12rpx);
  border-radius: 2rpx;
}

.textarea-input:focus {
  border-color: rgba(107, 142, 35, 0.5);
  outline: none;
  box-shadow: 0 0 0 4rpx rgba(107, 142, 35, 0.1), inset 0 2rpx 4rpx rgba(0, 0, 0, 0.05);
  transform: scale(1.02);
  background: linear-gradient(135deg, #ffffff 0%, #f8f9ff 100%);
}

.picker-input {
  width: 100%;
  padding: 30rpx; /* 增大内边距 */
  border: 3rpx solid #CCCCCC; /* 高对比度边框 */
  border-radius: 20rpx; /* 增大圆角 */
  font-size: 36rpx; /* 增大字体 */
  background: #FFFFFF; /* 纯色背景 */
  box-sizing: border-box;
  transition: all 0.3s ease; /* 简化动画 */
  cursor: pointer;
  position: relative;
  box-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.1); /* 更明显阴影 */
  min-height: 100rpx; /* 增大触摸高度 */
}

.picker-input::after {
  content: '📝';
  position: absolute;
  right: 20rpx;
  top: 50%;
  transform: translateY(-50%);
  color: rgba(160, 82, 45, 0.6);
  font-size: 24rpx;
  transition: transform 0.3s ease;
  pointer-events: none;
}

.picker-input:hover {
  border-color: rgba(107, 142, 35, 0.5);
  box-shadow: 0 0 0 2rpx rgba(107, 142, 35, 0.1), inset 0 2rpx 4rpx rgba(0, 0, 0, 0.05);
  transform: translateY(-1rpx);
}

.picker-input.placeholder {
  color: #999;
}

.char-count {
  text-align: right;
  font-size: 28rpx; /* 增大字体 */
  color: #000000; /* 高对比度 */
  margin-top: 15rpx; /* 增大上边距 */
}

/* 语音输入容器 */
.voice-input-container {
  position: relative;
}

.voice-btn {
  position: absolute;
  right: 20rpx;
  bottom: 20rpx;
  padding: 20rpx 30rpx; /* 增大内边距 */
  border: 3rpx solid #006400; /* 高对比度边框 */
  border-radius: 25rpx; /* 增大圆角 */
  background: #FFFFFF; /* 纯色背景 */
  color: #006400; /* 高对比度绿色 */
  font-size: 32rpx; /* 大字体 */
  box-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.2); /* 更明显阴影 */
  min-height: 80rpx; /* 增大触摸高度 */
  min-width: 120rpx; /* 增大触摸宽度 */
}

/* 生成按钮容器 */
.generate-button-container {
  margin-top: auto;
  padding-top: 30rpx;
  padding-bottom: 20rpx;
}

.btn-generate {
  width: 100%;
  padding: 35rpx; /* 增大内边距 */
  border: none;
  border-radius: 50rpx;
  font-size: 36rpx; /* 增大字体 */
  font-weight: bold;
  color: white;
  background: linear-gradient(45deg, #6B8E23, #A0522D);
  cursor: pointer;
  position: relative;
  overflow: hidden;
  box-shadow: 0 8rpx 25rpx rgba(0, 0, 0, 0.15), inset 0 2rpx 4rpx rgba(255, 255, 255, 0.1);
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  min-height: 120rpx; /* 设置最小高度，增大触摸区域 */
}

.btn-generate.loading {
  animation: buttonPulse 1.5s ease-in-out infinite;
}

.btn-generate:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  background: linear-gradient(45deg, #888, #666);
}

.btn-generate:not(:disabled):active {
  transform: scale(1.03);
  animation: buttonVibrate 0.3s ease-in-out;
}

@keyframes buttonPulse {
  0%, 100% {
    transform: scale(1);
    box-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.2);
  }
  50% {
    transform: scale(1.02);
    box-shadow: 0 15rpx 40rpx rgba(0, 0, 0, 0.3);
  }
}

.button-content {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 15rpx;
}

/* 加载动画 */
.loading-spinner {
  width: 40rpx;
  height: 40rpx;
  border: 4rpx solid rgba(255, 255, 255, 0.3);
  border-top: 4rpx solid white;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

/* 结果区域 */
.result-section {
  margin-top: 30rpx;
}

.error-container {
  background: #fee;
  border: 2rpx solid #fcc;
  border-radius: 15rpx;
  padding: 30rpx;
  text-align: center;
  animation: errorSlideIn 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  transform: translateX(0);
}

@keyframes errorSlideIn {
  from {
    opacity: 0;
    transform: translateX(-30rpx);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.error-message {
  color: #c33;
  font-size: 28rpx;
}

.result-container {
  animation: resultSlideIn 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes resultSlideIn {
  from {
    opacity: 0;
    transform: translateY(40rpx) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.result-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 30rpx;
  flex-wrap: wrap;
  gap: 20rpx;
  width: 100%; /* 确保标题区域占据容器的全部宽度 */
}

.result-title {
  font-size: 40rpx; /* 增大字体 */
  font-weight: bold;
  color: #000000; /* 高对比度 */
  flex: 1; /* 允许标题占据可用空间 */
  min-width: 200rpx; /* 设置最小宽度确保标题可读性 */
}

.result-actions {
  display: flex;
  gap: 20rpx;
  flex-wrap: wrap; /* 允许按钮换行 */
  justify-content: flex-end; /* 按钮右对齐 */
  flex: 1; /* 允许按钮区域占据可用空间 */
  min-width: 300rpx; /* 设置最小宽度确保按钮可见 */
}

/* 在小屏幕上调整按钮布局 */
@media (max-width: 768px) {
  .result-actions {
    flex-direction: column; /* 在小屏幕上垂直排列按钮 */
    align-items: stretch; /* 按钮拉伸以填充容器宽度 */
  }

  .action-btn {
    min-width: auto; /* 移除最小宽度限制 */
    width: 100%; /* 按钮占据容器的全部宽度 */
  }
}

.action-btn {
  padding: 30rpx 45rpx; /* 进一步增大内边距 */
  border: 3rpx solid #006400; /* 高对比度边框 */
  border-radius: 30rpx; /* 增大圆角 */
  background: #FFFFFF; /* 纯色背景 */
  color: #006400; /* 高对比度绿色 */
  font-size: 36rpx; /* 增大字体 */
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.2); /* 更明显阴影 */
  min-height: 120rpx; /* 增大触摸高度 */
  flex: 1; /* 使用flex布局，让按钮自动分配宽度 */
  display: flex;
  align-items: center;
  justify-content: center;
}

.action-btn:active {
  background: #6B8E23;
  color: white;
  transform: scale(1.03);
  animation: buttonVibrate 0.3s ease-in-out;
}

.blessing-content {
  font-size: 40rpx; /* 增大字体 */
  line-height: 1.8; /* 增加行距 */
  color: #000000; /* 高对比度 */
  padding: 40rpx; /* 增大内边距 */
  background: #FFFFFF; /* 纯色背景 */
  border: 3rpx solid #CCCCCC; /* 高对比度边框 */
  border-radius: 20rpx; /* 增大圆角 */
  white-space: pre-wrap;
  word-wrap: break-word;
  animation: contentReveal 0.8s ease 0.3s both; /* 简化动画 */
  position: relative;
  overflow: hidden;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.15); /* 更明显阴影 */
  font-family: 'Source Han Serif SC', 'KaiTi', '楷体', serif;
  text-align: center;
}

.blessing-content::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.6), transparent);
  animation: shimmer 1.5s ease-in-out 1s;
}

.blessing-content::after {
  content: '';
  position: absolute;
  top: 10rpx;
  right: 10rpx;
  width: 20rpx;
  height: 20rpx;
  background: linear-gradient(-45deg, transparent 6rpx, rgba(160, 82, 45, 0.3) 6rpx, rgba(160, 82, 45, 0.3) 8rpx, transparent 8rpx);
  border-radius: 2rpx;
}

@keyframes contentReveal {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes shimmer {
  0% { left: -100%; }
  100% { left: 100%; }
}

.copy-success {
  margin-top: 20rpx;
  padding: 20rpx;
  background: #d4edda;
  border: 2rpx solid #c3e6cb;
  border-radius: 10rpx;
  color: #155724;
  text-align: center;
  font-size: 28rpx;
  animation: copySuccessBounce 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  transform: scale(1);
}

.copy-success.fade-out {
  animation: copySuccessFadeOut 0.5s ease-out forwards;
}

@keyframes copySuccessBounce {
  0% {
    opacity: 0;
    transform: scale(0.3);
  }
  50% {
    opacity: 1;
    transform: scale(1.05);
  }
  70% {
    transform: scale(0.9);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes copySuccessFadeOut {
  from {
    opacity: 1;
    transform: scale(1);
  }
  to {
    opacity: 0;
    transform: scale(0.95);
  }
}

.empty-state {
  text-align: center;
  padding: 80rpx 40rpx;
  background: rgba(255, 255, 255, 0.8);
  border-radius: 20rpx;
}

.empty-icon {
  font-size: 80rpx;
  margin-bottom: 30rpx;
}

.empty-text {
  font-size: 32rpx;
  color: #666;
}

/* 无障碍支持 */
.sr-only {
  position: absolute;
  width: 1rpx;
  height: 1rpx;
  padding: 0;
  margin: -1rpx;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

/* 表单验证反馈动画 */
.form-error {
  animation: formShake 0.5s cubic-bezier(0.36, 0.07, 0.19, 0.97);
}

@keyframes formShake {
  0%, 100% { transform: translateX(0); }
  10%, 30%, 50%, 70%, 90% { transform: translateX(-5rpx); }
  20%, 40%, 60%, 80% { transform: translateX(5rpx); }
}

/* 成功提交反馈 */
.form-success {
  animation: formSuccessGlow 0.6s ease-out;
}

@keyframes formSuccessGlow {
  0% {
    box-shadow: 0 0 0 0 rgba(102, 126, 234, 0.4);
  }
  50% {
    box-shadow: 0 0 0 10rpx rgba(102, 126, 234, 0.2);
  }
  100% {
    box-shadow: 0 0 0 0 rgba(102, 126, 234, 0);
  }
}

/* 长按菜单样式 */
.long-press-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  animation: fadeIn 0.3s ease-out;
}

.long-press-menu {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 15rpx;
  padding: 20rpx;
  box-shadow: 0 10rpx 30rpx rgba(0, 0, 0, 0.2);
  min-width: 300rpx;
  animation: menuSlideIn 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.menu-item {
  padding: 30rpx 20rpx;
  border-bottom: 1rpx solid rgba(107, 142, 35, 0.2);
  cursor: pointer;
  transition: all 0.3s ease;
  text-align: center;
  font-size: 28rpx;
  color: #5A5A5A;
}

.menu-item:last-child {
  border-bottom: none;
}

.menu-item:active {
  background: rgba(107, 142, 35, 0.1);
  transform: scale(0.98);
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes menuSlideIn {
  from {
    opacity: 0;
    transform: scale(0.8) translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: scale(1) translateY(0);
  }
}

/* 信封飞出动画 */
.envelope-fly {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 60rpx;
  z-index: 1001;
  animation: envelopeFly 1.5s ease-out forwards;
  pointer-events: none;
}

@keyframes envelopeFly {
  0% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1) rotate(0deg);
  }
  50% {
    opacity: 1;
    transform: translate(-50%, -50%) scale(1.2) rotate(10deg);
  }
  100% {
    opacity: 0;
    transform: translate(-50%, -50%) scale(0.5) rotate(-20deg) translateX(200rpx) translateY(-200rpx);
  }
}

/* 响应式设计 */
@media (max-width: 768px) {
  .container {
    padding: 20rpx 10rpx;
  }

  .guide-text {
    font-size: 28rpx;
  }

  .guide-subtitle {
    font-size: 24rpx;
  }

  .card-primary {
    padding: 20rpx;
    margin-bottom: 20rpx;
  }

  .mode-buttons {
    gap: 10rpx;
  }

  .btn-mode {
    padding: 15rpx 20rpx;
    font-size: 24rpx;
    min-width: 150rpx;
  }

  .result-header {
    flex-direction: column;
    align-items: stretch;
  }

  .result-actions {
    justify-content: center;
  }

  /* 在小屏幕上进一步优化结果区域 */
  .result-actions {
    flex-direction: column; /* 在小屏幕上垂直排列按钮 */
    align-items: stretch; /* 按钮拉伸以填充容器宽度 */
    width: 100%; /* 确保按钮区域占据容器的全部宽度 */
  }

  .action-btn {
    min-width: auto; /* 移除最小宽度限制 */
    width: 100%; /* 按钮占据容器的全部宽度 */
    margin-bottom: 15rpx; /* 增加按钮之间的间距 */
  }

  .result-title {
    text-align: center; /* 在小屏幕上居中标题 */
    min-width: auto; /* 移除最小宽度限制 */
    margin-bottom: 20rpx; /* 增加与按钮的间距 */
  }
}